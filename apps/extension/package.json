{
  "name": "ali-sum-extension",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "node --env-file-if-exists=.env build.mjs",
    "watch": "node --env-file-if-exists=.env build.mjs --watch",
    "clean": "rm -rf dist",
    "bld": "npm -w apps/extension run build"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.0.0",
    "@types/chrome": "^0.0.300"
  }
}