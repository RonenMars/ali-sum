{
  "name": "ali-sum-extension",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.0.0",
    "@types/chrome": "^0.0.300"
  }
}
